# Keep dependency resolution strict so apps can only import what they declare.
shamefully-hoist=false
# Peer ranges in this stack move fast; warn instead of failing installs.
strict-peer-dependencies=false
